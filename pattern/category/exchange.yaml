# 类型
category:
  # 名称
  name: exchange
  # 意图序列
  intention-sequences:
    # 只需要写核心步骤，不需要写start、end
    # swap
    -
      - connect-wallet
      - input-form
      - swap
    # approve
    -
      - connect-wallet
      - input-form
      - approve